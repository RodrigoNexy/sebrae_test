# Variáveis opcionais para docker-compose (raiz do repositório).
# Copie para `.env` na raiz se quiser sobrescrever os padrões.

APP_PORT=8000
APP_ENV=local
APP_DEBUG=true

DB_DATABASE=laravel
DB_USERNAME=laravel
DB_PASSWORD=secret
MYSQL_ROOT_PASSWORD=rootsecret
FORWARD_DB_PORT=3306
